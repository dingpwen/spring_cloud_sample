spring:
  application:
    name: zipkin-server

server:
  port: 9000
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
      
management:
  metrics:
    web:
      server:
        auto-time-requests: false